# Local development settings (defaults match these if unset).
DEBUG=True
SECRET_KEY=change-me-to-a-long-random-string
ALLOWED_HOSTS=localhost,127.0.0.1

# Production (Render) — set these in the Render dashboard:
# DATABASE_URL=postgres://...
# CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME
# CORS_ALLOWED_ORIGINS=https://fpc-website.vercel.app
# CSRF_TRUSTED_ORIGINS=https://fpc-website.vercel.app
# ADMIN_PASSWORD=strong-admin-password
# EDITOR_PASSWORD=strong-editor-password
