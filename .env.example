# Optional: the deployed Django backend origin (Render).
# Set VITE_API_URL in Vercel (Project > Settings > Environment Variables) and
# redeploy. If unset, the app calls the same origin (Vite dev proxy / same host).
VITE_API_URL=https://fpc-backend.onrender.com
